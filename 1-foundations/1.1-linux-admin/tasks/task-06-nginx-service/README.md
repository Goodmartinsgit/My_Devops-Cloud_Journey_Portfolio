# Task 6: Install and Manage Nginx as a Systemd Service

## Objective

Install the Nginx web server, practice systemd service daemon controls (start, stop, reload, restart), configure automatic startup on boot, and verify access via local requests.

<!-- LOG_OUTPUT -->
