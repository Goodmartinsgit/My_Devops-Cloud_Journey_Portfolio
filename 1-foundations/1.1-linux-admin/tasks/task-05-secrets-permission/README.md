# Task 5: Create a Secrets File with Permission 600

## Objective

Harden sensitive database credentials by setting their file access permissions to `600` (`-rw-------`), ensuring only the owner can read/write the credentials and other processes are denied access.

<!-- LOG_OUTPUT -->
