
---

## creating and setting password for  devops user account

> **Timestamp:** `2026-06-11 10:41:29`

### 01. `sudo useradd -m -s /bin/bash -c "Devops User" devops-user && sudo passwd devops-user`

```bash
sudo useradd -m -s /bin/bash -c "Devops User" devops-user && sudo passwd devops-user
```

**Output:**

```text
New password: 
Retype new password: 
passwd: password updated successfully
```


---

## adding devops-user to sudo group with full passwordless sudo privileges configuration

> **Timestamp:** `2026-06-11 12:02:15`

### 01. `sudo usermod -aG sudo devops-user && echo "devops-user ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/devops-user && sudo chmod 440 /etc/sudoers.d/devops-user`

```bash
sudo usermod -aG sudo devops-user && echo "devops-user ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/devops-user && sudo chmod 440 /etc/sudoers.d/devops-user
```

**Output:**

```text
devops-user ALL=(ALL) NOPASSWD: ALL
```


---

## Testing and verifying the users account

> **Timestamp:** `2026-06-11 12:18:45`

### 01. `su - devops-user && sudo whoami && sudo apt update && id && grep devops-user /etc/passwd && id devops-user && group devops-user`

```bash
su - devops-user && sudo whoami && sudo apt update && id && grep devops-user /etc/passwd && id devops-user && group devops-user
```

**Output:**

```text
Password: 
-bash: line 1: July19th: command not found
-bash: error reading input file: Input/output error

---

## switching ti devops-user account

> **Timestamp:** `2026-06-11 12:30:44`

### 01. `su - devops-user`

```bash
su - devops-user
```

**Output:**

```text
Password: 
Hangup

----


## Verifying devops-user account

> **Timestamp:** `2026-06-13 08:31:07`

### 01. `id devops-user`

```bash
id devops-user
```

**Output:**

```text
uid=1001(devops-user) gid=1001(devops-user) groups=1001(devops-user),27(sudo)
```

### 02. `grep devops-user /etc/passwd`

```bash
grep devops-user /etc/passwd
```

**Output:**

```text
devops-user:x:1001:1001:Devops User:/home/devops-user:/bin/bash
```

### 03. `groups devops-user`

```bash
groups devops-user
```

**Output:**

```text
devops-user : devops-user sudo
```

### 04. `sudo -u devops-user whoami`

```bash
sudo -u devops-user whoami
```

**Output:**

```text
devops-user
```

### 05. `sudo -u devops-user id`

```bash
sudo -u devops-user id
```

**Output:**

```text
uid=1001(devops-user) gid=1001(devops-user) groups=1001(devops-user),27(sudo)
```

===================================================================================
===================================================================================

