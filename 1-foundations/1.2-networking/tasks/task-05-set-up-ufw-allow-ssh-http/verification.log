
---

##  Installed UFW and set default policies: deny all incoming, allow all outgoing

> **Timestamp:** `2026-07-06 15:54:39`

### 01. `sudo apt install ufw -y`

```bash
sudo apt install ufw -y
```

**Output:**

```text

WARNING: apt does not have a stable CLI interface. Use with caution in scripts.

Reading package lists...
Building dependency tree...
Reading state information...
ufw is already the newest version (0.36.2-6).
0 upgraded, 0 newly installed, 0 to remove and 132 not upgraded.
```

### 02. `sudo ufw default deny incoming`

```bash
sudo ufw default deny incoming
```

**Output:**

```text
Default incoming policy changed to 'deny'
(be sure to update your rules accordingly)
```

### 03. `sudo ufw default allow outgoing`

```bash
sudo ufw default allow outgoing
```

**Output:**

```text
Default outgoing policy changed to 'allow'
(be sure to update your rules accordingly)
```

### 04. `sudo ufw status`

```bash
sudo ufw status
```

**Output:**

```text
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere                  
80/tcp                     ALLOW       Anywhere                  
443/tcp                    ALLOW       Anywhere                  
22/tcp (v6)                ALLOW       Anywhere (v6)             
80/tcp (v6)                ALLOW       Anywhere (v6)             
443/tcp (v6)               ALLOW       Anywhere (v6)             

```


---

##  Allowed inbound traffic on SSH (22), HTTP (80), and HTTPS (443)

> **Timestamp:** `2026-07-06 15:55:26`

### 01. `sudo ufw allow 22/tcp`

```bash
sudo ufw allow 22/tcp
```

**Output:**

```text
Skipping adding existing rule
Skipping adding existing rule (v6)
```

### 02. `sudo ufw allow 80/tcp`

```bash
sudo ufw allow 80/tcp
```

**Output:**

```text
Skipping adding existing rule
Skipping adding existing rule (v6)
```

### 03. `sudo ufw allow 443/tcp`

```bash
sudo ufw allow 443/tcp
```

**Output:**

```text
Skipping adding existing rule
Skipping adding existing rule (v6)
```

### 04. `sudo ufw status numbered`

```bash
sudo ufw status numbered
```

**Output:**

```text
Status: active

     To                         Action      From
     --                         ------      ----
[ 1] 22/tcp                     ALLOW IN    Anywhere                  
[ 2] 80/tcp                     ALLOW IN    Anywhere                  
[ 3] 443/tcp                    ALLOW IN    Anywhere                  
[ 4] 22/tcp (v6)                ALLOW IN    Anywhere (v6)             
[ 5] 80/tcp (v6)                ALLOW IN    Anywhere (v6)             
[ 6] 443/tcp (v6)               ALLOW IN    Anywhere (v6)             

```


---

##  Enabled UFW firewall and verified active rules for ports 22, 80, 443

> **Timestamp:** `2026-07-06 15:55:43`

### 01. `sudo ufw --force enable`

```bash
sudo ufw --force enable
```

**Output:**

```text
Firewall is active and enabled on system startup
```

### 02. `sudo ufw status verbose`

```bash
sudo ufw status verbose
```

**Output:**

```text
Status: active
Logging: on (low)
Default: deny (incoming), allow (outgoing), disabled (routed)
New profiles: skip

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW IN    Anywhere                  
80/tcp                     ALLOW IN    Anywhere                  
443/tcp                    ALLOW IN    Anywhere                  
22/tcp (v6)                ALLOW IN    Anywhere (v6)             
80/tcp (v6)                ALLOW IN    Anywhere (v6)             
443/tcp (v6)               ALLOW IN    Anywhere (v6)             

```


---

##  Tested firewall rules: ports 22/80/443 expected open, port 8080 expected blocked

> **Timestamp:** `2026-07-06 16:01:11`

### 01. `nc -zv localhost 22`

```bash
nc -zv localhost 22
```

**Output:**

```text
Connection to localhost (127.0.0.1) 22 port [tcp/ssh] succeeded!
```

### 02. `nc -zv localhost 80`

```bash
nc -zv localhost 80
```

**Output:**

```text
Connection to localhost (127.0.0.1) 80 port [tcp/http] succeeded!
```

### 03. `nc -zv localhost 443`

```bash
nc -zv localhost 443
```

**Output:**

```text
nc: connect to localhost (127.0.0.1) port 443 (tcp) failed: Connection refused
```

### 04. `nc -zv -w 3 localhost 8080`

```bash
nc -zv -w 3 localhost 8080
```

**Output:**

```text
nc: connect to localhost (127.0.0.1) port 8080 (tcp) failed: Connection refused
```

### 05. `echo 'Port 8080 test exit code: '0`

```bash
echo 'Port 8080 test exit code: '0
```

**Output:**

```text
Port 8080 test exit code: 0
```

